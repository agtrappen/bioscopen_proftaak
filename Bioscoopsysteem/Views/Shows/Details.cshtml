@model Bioscoopsysteem.Models.Shows

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Shows</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Start_date)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Start_date)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ShowsId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
