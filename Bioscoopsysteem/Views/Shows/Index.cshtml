@model IEnumerable<Bioscoopsysteem.Models.Shows>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Start_date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Start_date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ShowsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ShowsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ShowsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
